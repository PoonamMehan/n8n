

export const WorkflowProvider = ({workflowsData, credentialsData, children}: {workflowsData: any, credentialsData: any, children: React.ReactNode})=>{
  
  return(
    <>
      {children}
    </>
  )
}